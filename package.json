{
  "name": "eventbus",
  "description": "Simple ES6-ready class for managing events in JavaScript",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c --watch",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "1.0.0-rc.1",
    "rollup": "^0.66.6",
    "rollup-plugin-terser": "^3.0.0"
  }
}
